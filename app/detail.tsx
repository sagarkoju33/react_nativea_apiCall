import { Text, View } from "react-native";
//

const Details = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ color: "red", fontSize: 60 }}>Hello Fucker</Text>
    </View>
  );
};

export default Details;
